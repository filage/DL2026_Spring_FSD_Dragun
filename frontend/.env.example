# Frontend
# Public token for Mapbox GL JS (will be embedded in the client bundle).
# Create token in Mapbox dashboard and set it here.
PUBLIC_MAPBOX_ACCESS_TOKEN=

# Backend base URL (optional)
PUBLIC_API_BASE_URL=http://localhost:5174/api
